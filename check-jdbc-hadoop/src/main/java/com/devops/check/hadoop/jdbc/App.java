package com.devops.check.hadoop.jdbc;import com.devops.check.hadoop.jdbc.utils.JdbcSql;import java.sql.Connection;import java.sql.DriverManager;import java.sql.ResultSet;import java.sql.Statement;import java.util.Properties;import java.io.FileInputStream;import java.io.FileNotFoundException;import java.io.IOException;public class App {    public static void main(String[] args) throws FileNotFoundException, IOException {        //if (args.length < 2 || args[0] != "--file" ) {        if (args.length < 2 || !"--file".equals(args[0])) {            System.err.println("Please provide the path to config file. eg: --file application.properties");            System.exit(1);        }        final String configFilePath = args[1];        final Properties properties = new Properties();        properties.load(new FileInputStream(configFilePath));        //String appVersion = properties.getProperty("app.version");        final String appJdbcUrl = properties.getProperty("app.jdbc.url");        final String appSql = properties.getProperty("app.sql");        final String appDriverName = properties.getProperty("app.driver.name");        JdbcSql sparkSql = new JdbcSql();        sparkSql.setConnectionUrl(appJdbcUrl);        sparkSql.setDriverName(appDriverName);        sparkSql.setSql(appSql);        sparkSql.excuteSql();    }}